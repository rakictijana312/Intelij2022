package JavaTeorija;

public class Method {

    /* Method is the collection of statements that they are group together to preform an operation.
    Method can be called from any point of the program by method name.


       Main method is the starting point of any application. Whenever program execute, main method is first to get
       execute. Without main method we can compile (transforming byte/machine code to optimize the code)

     */
}
